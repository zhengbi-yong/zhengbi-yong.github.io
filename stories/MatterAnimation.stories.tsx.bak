import type { Meta, StoryObj } from '@storybook/nextjs'
import MatterAnimation from '@/components/MatterAnimation.tsx'

const meta: Meta<typeof MatterAnimation> = {
  title: 'components/MatterAnimation',
  component: MatterAnimation,
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],
}

export default meta
type Story = StoryObj<typeof meta>

export const Default: Story = {
  args: {},
}
