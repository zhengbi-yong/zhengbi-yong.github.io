import type { Meta, StoryObj } from '@storybook/nextjs'
import LazyLoadedComponents from '@/components/LazyLoadedComponents.tsx'

const meta: Meta<typeof LazyLoadedComponents> = {
  title: 'components/LazyLoadedComponents',
  component: LazyLoadedComponents,
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],
}

export default meta
type Story = StoryObj<typeof meta>

export const Default: Story = {
  args: {},
}
