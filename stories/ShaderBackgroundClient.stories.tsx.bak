import type { Meta, StoryObj } from '@storybook/nextjs'
import ShaderBackgroundClient from '@/components/ShaderBackgroundClient.tsx'

const meta: Meta<typeof ShaderBackgroundClient> = {
  title: 'components/ShaderBackgroundClient',
  component: ShaderBackgroundClient,
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],
}

export default meta
type Story = StoryObj<typeof meta>

export const Default: Story = {
  args: {},
}
