import type { Meta, StoryObj } from '@storybook/nextjs'
import PinElement from '@/components/animations/PinElement.tsx'

const meta: Meta<typeof PinElement> = {
  title: 'components/PinElement',
  component: PinElement,
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],
}

export default meta
type Story = StoryObj<typeof meta>

export const Default: Story = {
  args: {},
}
