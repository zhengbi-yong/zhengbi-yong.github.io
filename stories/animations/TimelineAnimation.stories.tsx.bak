import type { Meta, StoryObj } from '@storybook/nextjs'
import TimelineAnimation from '@/components/animations/TimelineAnimation.tsx'

const meta: Meta<typeof TimelineAnimation> = {
  title: 'components/TimelineAnimation',
  component: TimelineAnimation,
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],
}

export default meta
type Story = StoryObj<typeof meta>

export const Default: Story = {
  args: {},
}
