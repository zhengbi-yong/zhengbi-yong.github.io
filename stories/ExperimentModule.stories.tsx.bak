import type { Meta, StoryObj } from '@storybook/nextjs'
import ExperimentModule from '@/components/ExperimentModule.tsx'

const meta: Meta<typeof ExperimentModule> = {
  title: 'components/ExperimentModule',
  component: ExperimentModule,
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],
}

export default meta
type Story = StoryObj<typeof meta>

export const Default: Story = {
  args: {},
}
