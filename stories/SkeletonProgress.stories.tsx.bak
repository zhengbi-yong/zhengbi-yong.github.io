import type { Meta, StoryObj } from '@storybook/nextjs'
import SkeletonProgress from '@/components/SkeletonProgress.tsx'

const meta: Meta<typeof SkeletonProgress> = {
  title: 'components/SkeletonProgress',
  component: SkeletonProgress,
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],
}

export default meta
type Story = StoryObj<typeof meta>

export const Default: Story = {
  args: {},
}
