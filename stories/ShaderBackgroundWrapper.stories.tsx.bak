import type { Meta, StoryObj } from '@storybook/nextjs'
import ShaderBackgroundWrapper from '@/components/ShaderBackgroundWrapper.tsx'

const meta: Meta<typeof ShaderBackgroundWrapper> = {
  title: 'components/ShaderBackgroundWrapper',
  component: ShaderBackgroundWrapper,
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],
}

export default meta
type Story = StoryObj<typeof meta>

export const Default: Story = {
  args: {},
}
