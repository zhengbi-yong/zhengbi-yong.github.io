import type { Meta, StoryObj } from '@storybook/nextjs'
import Footer from '@/components/Footer'

const meta: Meta<typeof Footer> = {
  title: 'Components/Footer',
  component: Footer,
  parameters: {
    layout: 'fullscreen',
  },
  tags: ['autodocs'],
  decorators: [
    (Story) => (
      <div className="min-h-screen flex flex-col">
        <div className="flex-1 p-8">
          <h1 className="text-2xl font-bold mb-4">Page Content</h1>
          <p>This is the main content area. Scroll down to see the footer.</p>
          <div className="h-96 bg-gray-100 dark:bg-gray-800 rounded-lg mt-4 flex items-center justify-center">
            <p className="text-gray-500">Scroll for footer</p>
          </div>
        </div>
        <Story />
      </div>
    ),
  ],
}

export default meta
type Story = StoryObj<typeof meta>

// Default footer
export const Default: Story = {}

// Mobile footer view
export const Mobile: Story = {
  parameters: {
    viewport: {
      defaultViewport: 'mobile1',
    },
  },
}

// Dark mode footer
export const DarkMode: Story = {
  globals: {
    backgrounds: {
      value: "dark"
    }
  },
}