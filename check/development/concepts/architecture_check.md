# 审查报告：项目组件架构

> 审查时间：2026-05-05
> 文档路径：`docs/development/concepts/architecture.md`
> 文件大小：29.4 KB / 1264 行

## 审查结论：⚠️ 部分偏差

项目组件架构总览文档，描述了架构图和组件关系。

## 提到的文件（部分）

| 文件 | 状态 |
|------|------|
| `AdminLayout.tsx` | ❌ 不存在 |
| `AdminStatsCard.tsx` | ❌ 不存在 |
| `AntVChart.tsx` | ❌ 不存在 |
| `AudioPlayer.tsx` | ❌ 不存在 |
| `BlogCard.tsx` | ❌ 不存在 |
| `ChemicalStructure.tsx` | ❌ 不存在 |
| `CommentModeration.tsx` | ❌ 不存在 |
| `EChartsComponent.tsx` | ❌ 不存在 |

## 总结

- **总文件数**：46
- **✅ 存在**：约 0（0%）
- **❌ 不存在**：约 46（100%）
- **问题**：组件路径已重构，文档过时
